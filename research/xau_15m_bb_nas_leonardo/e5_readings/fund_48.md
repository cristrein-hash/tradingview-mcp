# Fund 48 — DEEP READING (XAU 15M MON+FORTE bottom)

- **Date / time:** 2025-01-13 19:00 (UTC stored `t`) — block 2024-11-25
- **Tier:** FORTE · power_score 4.0 · year 2025
- **leg_atr:** 19.6 · **mfe12:** 2.57 ATR · **mae12:** 0.01 ATR (near-zero adverse — a no-look-back leg)
- **Session:** LATE, `killzone=0` (formed OUTSIDE London/NY kill window — the off-killzone profile that Angle 1/Angle 3 flagged as the single cleanest MON+FORTE discriminator)

---

## (a) THE ENTRY MECHANIC — where/when I would actually enter

This is **not** a violent flush-and-snap; it is a **quiet absorption + climbing-floor staircase**. The mechanic, read causally bar-by-bar of the reaction (low = bar 0):

- **bar 0 (the low):** shallow sweep — `swept_prior_low=1` but `sweep_depth_atr=0.23` (extremely shallow stop-run, NOT a deep capitulation), `lower_wick_ratio=0.28` (small rejection wick), `low_closepos=0.28` (closes mid-low — no dramatic hammer). The low is made on a *failed-supply* footprint: 2 consec-down only, `downleg_eff=0.30` (grindy, inefficient descent), in a compressed/calm regime (`atr_regime=0.93`, `atr_compression_pre=1.47`).
- **bar +1:** green, `c_atr 0.56`, `l_atr 0.01` — first higher-... actually `first_higher_low_bar=1` confirms the higher-low forms immediately. This is the **first reclaim thrust**.
- **bar +2:** green, `c_atr 0.91`, `l_atr 0.38` — floor steps up, front-loaded (first two bars do +0.91 ATR of the reclaim).
- **bars +3..+6:** the only floor wobble is bar 3 (`l_atr 0.31`, one-tick dip below bar 2's 0.38) — a shallow held retest, then `l_atr` climbs monotonically 0.58→0.63→0.70.
- **bar +7:** `c_atr 1.59`, **EMA21 reclaimed** (`reclaim_ema_bars=7`) — momentum confirmation.

**WHERE I ENTER:** I would NOT chase the low bar (no decisive engulf/climax there). The clean, causal entry is the **micro higher-low + reclaim on bar +1/+2** — specifically a stop entry on bar +2 confirming the climbing floor (`first_higher_low_bar=1`, low stepping 0.01→0.38), with a tighter-conviction add on the **EMA21 reclaim at bar +7**. Trigger type = **shallow-sweep + immediate micro-HL reclaim (Angle 0 L7 liquidity_grab_no_followthrough), NOT a CHoCH** (`choch_15m_after=0`) and **NOT a NAS-long** (`nas_long_after=0`, `nas_long_16=0`). The leg ran to +2.57 ATR with essentially zero heat (mae 0.01) — entering on the +1/+2 reclaim captures almost the full leg.

---

## (b) LENSES PRESENT / STRONG here

**STRONG (the defining stack for this fund):**
- **Angle 0 L2 `quiet_climax` — 3/3 (textbook).** `vol_climax=0.67` (<1.35 ✓, very quiet), `sweep_depth_atr=0.23` (<1.8 ✓, ultra-shallow), `lower_wick_ratio=0.28` (<0.45 ✓). This is the *anti-capitulation* fingerprint — the low is made on whisper volume and a 0.23-ATR sweep. Pure MON+FORTE signature.
- **Angle 0 L7 / Angle 3 L4 `liquidity_grab_no_followthrough` / cross-session sweep:** `swept_prior_low=1` + shallow 0.23-ATR depth + immediate reclaim (HL on bar +1) = stop-run that failed to extend.
- **Angle 0 L1 `effort_vs_result_failure` / Angle 2 L5 inefficient drop:** `downleg_eff=0.30` (grindy), only `consec_down=2`, `drop20_atr=5.03` over a churny path = effort spent, little clean result = absorption.
- **Angle 2 / Angle 5 L5.4 `compressed_regime`:** `atr_regime=0.93` (<1.0 ✓ calm) AND `atr_compression_pre=1.47` (elevated ✓). Coiled-spring vol regime — exactly the "drained-and-coiled, not climactic" thesis.
- **Angle 1 L1 / Angle 3 QUIET RECLAIM (off-killzone):** `killzone=0`, LATE session — the 8.1×-lift off-killzone profile.
- **Angle 4 L1 `reclaim_low_monotone_k` / L5 clean R² / L8 shallow retest:** `l_atr` is near-monotone climbing (0.01→0.38→[0.31 micro-dip]→0.58→0.63→0.70→1.13...), `mae12=0.01` (the retest essentially never returns toward the low). Climbing-floor staircase confirmed.
- **Angle 1 L6 `discount-not-breakdown`:** `dealing_range_pos=-0.163` — sitting in the lower band of the dealing range but **NOT broken** (pos > -0.2 edge / well above -1.0). Discount accumulation, not a range break/continuation.
- **`in_demand=1` (both 15M and HTF):** `htf4_native.in_demand=1`, `htf1_native.in_demand=1`, `demand_virgin=1` — the flush lands on virgin demand defended across frames.

**MIXED / PARTIAL (honest):**
- **Angle 5 cross-TF momentum is NOT the classic MON template here.** `htf4_native.trend=-1` (4H still bearish ✓ phase-lag premise) BUT `htf1_native.trend=+1` (1H already turned up ✓ — the L5.1 phase-lag IS present). HOWEVER `htf1_native.in_demand=1` (1H still pinned in demand) and `features_E1.h1_trend=-1`, `h1_rsi=28.2` (15M-frame 1H RSI washed) — so the L5.2 "1H room-above" reading is WEAK/inverted on the E1 fields. Read it as: 1H native turned up while still at its floor — an early, not a clean-lift, phase-lag.
- **Angle 5 L5.6 stacked-demand-clean-sky:** `htf4_native.clean_sky_atr=0.01` (NO clean sky on 4H — supply right overhead) but `htf1_native.clean_sky_atr=1.18` (1H runway). `n_supply_overhead=100` and `dist_supply_atr=-0.26` (price actually inside/at supply too). So overhead is congested on 4H — the leg ran +2.57 ATR despite a capped 4H sky, which makes the **demand-defense + absorption** read the carrier, not "air above."
- **Daily backdrop bullish:** `hd_trend=0/macro_bull=1`, `hd_rsi=61`, `hd_slope_atr=4.53`, `hd_pos=0.63` — the higher-daily structure is constructive (this is a pullback-in-uptrend bottom, not a trend-reversal bottom).

**ABSENT / NOT the trigger (important for causality):**
- `nas_long_16=0`, `nas_long_after=0` — **no NAS confirmation.** (Angle 3 L6 / Angle 5 L7 do not fire.)
- `smc_bos=0`, `choch_15m_after=0` — **no CHoCH/BOS structural trigger.**
- `rsi_bull_div=0`; `buy_bub_w=0` (no buy-bubble first-print, Angle 0 L9 absent).
- `sell_bub_w=10`, `sell_decel=-2` — there WAS sell-bubble effort present (not the dried-up Angle 0 L3 ideal), but `sell_decel=-2` shows it was *decelerating* into the low. So the absorption read holds, just not the cleanest "bubble desert" variant.

---

## (c) WHAT IS DISTINCTIVE about this bottom

1. **Pure quiet-absorption, zero-heat leg.** `mae12=0.01 ATR` is the headline: once it turned, it never looked back. The entry on the +1/+2 reclaim had essentially no drawdown — a rare, ultra-clean climbing-floor staircase.
2. **It is a TEXTBOOK 3/3 `quiet_climax`** — the most distinctive single lens. Volume 0.67, sweep 0.23 ATR, wick 0.28 — about as far from capitulation theatrics as a strong bottom gets. This fund is a poster-child for the "invert the climax detector" thesis.
3. **No NAS, no CHoCH, no buy-bubble, no RSI-divergence.** Distinctively, this monster bottom carries **none** of the popular discrete triggers. Its edge lives entirely in the *quiet-absorption + shallow-sweep + climbing-floor + compressed-regime + off-killzone* convergence — a "context, not signal" bottom.
4. **Grindy 2-bar descent (not a flush).** `consec_down=2`, `downleg_eff=0.30` — it didn't crash in; it ground into a compressed pocket and sprang. Confirms the Angle 2 drained-and-coiled prior.

---

## (d) MACRO / HTF CONTEXT

- **Daily (1D):** constructive uptrend backdrop — `hd_trend=0` but `macro_bull=1`, `hd_slope_atr=+4.53`, `hd_rsi=61`, `hd_pos=0.63`, `hd_eff=0.57`. This is a **pullback INTO a bullish daily**, the highest-quality context for a long.
- **4H:** `htf4_native.trend=-1` (native) but `features_E1.h4_trend=+1`, `h4_rsi=44.8`, `h4_pos=0.22` — 4H is a mid-range pullback, demand-tagged (`in_demand=1`), with supply right overhead (`clean_sky_atr=0.01`). The leg had to *earn* its run against a capped 4H ceiling — done via absorption.
- **1H:** native trend just `+1` (phase-lag turn present) while still in/near demand and 15M-frame 1H RSI washed (28). The fast frame is curling up first.
- **Session:** LATE / off-killzone, 2025-01-13 19:00 — quiet liquidity window, consistent with the engineered-quiet-reversal profile rather than a London/NY crowd flush.
- **Net macro read:** bullish-daily pullback → quiet shallow stop-run into virgin multi-TF demand → controlled absorption (compressed vol, grindy inefficient descent, decelerating sell effort) → immediate climbing-floor reclaim with zero heat. A **demand-defense / quiet-absorption** monster, NOT a capitulation-V and NOT a trigger-driven (NAS/CHoCH/bubble) one.

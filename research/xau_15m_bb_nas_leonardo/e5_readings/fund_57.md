# Fund 57 — DEEP READING

**Block** 2025-05-25 · **Date** 2025-08-20 01:15 UTC · **Tier** FORTE · **leg_atr** 17.0 · **power_score** 5.4 · **Session** ASIA · **killzone** 0

---

## (a) THE ENTRY MECHANIC — where/when I actually enter

This is a **same-bar sweep + instant reclaim** bottom — the cleanest, earliest entry type.

- `swept_prior_low = 1`, `sweep_depth_atr = 0.53` → the low bar pierced a prior fractal/local low by only **half an ATR** (shallow grab, not a deep flush).
- `reclaim_ema_bars = 1`, `first_higher_low_bar = 1`, `choch_15m_after = 1` → EMA21 is reclaimed on the **very first reaction bar**, which is also the first higher-low, and a 15M bullish CHoCH prints right after.
- `reaction_seq` bar 1: `l_atr 0.08`, `c_atr 2.36`, `green = 1` → the bottom bar makes the low at the very bottom of its range and **closes +2.36 ATR up** = a wide-range bullish **engulf/thrust** off the low (Angle 4 L6 `pivot_engulf_thrust`, L9 `velocity_regime_flip`). MAE over 12 bars = 0.08 ATR: **price never returns to the low**.

**Entry decision (causal, info ≤ entry bar):**
- **Primary (aggressive):** enter on the CLOSE of reaction bar 1 — it swept a prior low, reclaimed EMA21, closed strong (+2.36 ATR engulf), and the 15M CHoCH-up confirms. This is the textbook "shallow-grab + same-bar reclaim + thrust" trigger.
- **Confirmation/retest (preferred for risk):** bars 2–6 are a tight **coil/retest** that holds well above the low (`l_atr` 1.54 → 0.7 → 0.95 → 0.49 → 0.37, never back to 0.08 — a shallow higher-low base, Angle 4 L8 `reclaim_dip_depth` shallow). A re-entry or add on the bar-7 break (`c_atr 2.21`, green) when the coil resolves up is the lower-risk version. The **staircase resumes hard** bars 7→11 (2.21→2.46→2.68→3.10) to MFE 3.34.
- **Stop:** below the bar-1 low (the swept extreme). MAE 0.08 means a stop a few ticks under the low is never threatened — this is a high-quality, defended floor.

So: **trigger = shallow sweep-of-prior-low + same-bar EMA reclaim + bullish engulf thrust + 15M CHoCH.** Enter bar-1 close, or on the bar-7 coil-break for confirmation.

## (b) Lenses PRESENT / STRONG here

**Quiet-absorption fingerprint (Angles 0, 1, 2 — STRONG, this is the archetype):**
- `sweep_depth_atr 0.53` (very shallow), `vol_climax 1.12` (modest), `downleg_eff 0.31` (grindy/inefficient descent), `atr_compression_pre 0.69`, `atr_regime 0.88` (calm regime), `lower_wick_ratio 0.17` (small rejection wick), `consec_down 2` (short final push). → Angle 0 **L2 quiet_climax** (modest vol + shallow sweep + small wick = the MONFORTE inverse-capitulation), Angle 2 **L1 atr_decel / coiled-spring** (calm, compressed). This bottom is born of *quiet controlled absorption*, not a violent flush — exactly the strong-bottom archetype.

**Sell-effort decelerating (Angle 0 L3):** `sell_bub_w 8` + `sell_decel 8`, `buy_bub 0` → sell-bubble effort was present but **decelerating** into the low (`sell_decel` high) while no fresh buy bubbles — supply footprint fading. NAS-LONG present (`nas_long_16 = 1`).

**Momentum absorption / hidden divergence (Angle 0 L10, Angle 5 L5.3):** `rsi_low 35.9`, `rsi_min8 30.9` (NOT deeply oversold), `rsi_bull_div 1`, `rsi_head 0.85` → RSI refused to break down to a panic-oversold reading while price made the low = momentum absorbed.

**Time / session (Angle 3 — STRONG):** **ASIA session, hour 01:15 UTC, killzone = 0.** This hits the single strongest discriminator in the catalog: off-killzone + Asia-ramp (01:00 UTC = 4.7× enriched, Asia 22–03 UTC = 2.3× enriched). A clean reversal engineered in thin Asia liquidity (Angle 3 L1/L3 `asia_offpeak_flush` / first-session-hour).

**Inter-bar geometry (Angle 4 — STRONG):** bar-1 engulf thrust (L6), hard slope-flip (L9), shallow-retest coil that holds (L8), then a monotone staircase bars 7–12 (L1/L5 clean-ramp). `mfe12 3.34 / mae12 0.08` = textbook no-look-back launch.

**Structure (Angle 5):** `smc_bos 1`, `choch_15m_after 1`, 4H `choch_rec 1`. Cross-TF structure flip-up confirmed on 15M with a recent 4H CHoCH.

**Lenses ABSENT / WEAK / against-type (honest):**
- **Angle 5 L5.1/L5.2 (1H-room-above / 1H-leads-4H turn) is NOT present.** `htf1_native.trend = -1`, `in_demand = 1`, `dist_demand_atr = -0.12`, `h1_pos = -0.02` → the 1H is **still bearish and still pinned inside/below its demand**, NOT lifted off the floor. This bottom does NOT show the "1H already turned" cross-TF spring. Both 4H and 1D are also `trend = -1`. So the regime-onset triad (Angle 5 #1) FAILS here — the edge lives in the 15M absorption + Asia-timing + shallow-sweep mechanic, not in HTF leading.
- `flush_v_ratio 0.37` (moderate, not a sharp V), `dealing_range_pos -0.271` (discount band — Angle 1 L6 OK), `vpnode_dist_atr -10.76` (far below value node), `n_supply_overhead 528` (heavy overhead — NOT thin sky; Angle 1 L3 asymmetry is weak here).

## (c) What is DISTINCTIVE about this bottom

It is a **deep-discount, HTF-against-trend Asia reversal that works purely on local mechanics**, not HTF confluence. Every higher frame (1H/4H/1D) is still bearish and the 15M is flushed into demand while the 1H is pinned inside its own demand — by the Angle-5 cross-TF logic this "should" be a continuation/weak low. Instead it is FORTE because the **micro-mechanic is impeccable**: shallow sweep (0.53 ATR), same-bar EMA reclaim + bullish engulf thrust, RSI holding above the floor with bull-div, sell-effort decelerating, all in the **01:15 UTC Asia off-killzone vacuum** — a forced-liquidation snap-back into thin liquidity. MAE 0.08 ATR proves the floor was defended from the first bar. The distinctive tell: **it does NOT need HTF agreement; it needs the quiet-absorption + Asia-timing + clean-thrust stack.** This is a strong counter-example to over-weighting the HTF phase-lag lens.

## (d) Macro / HTF context

- **4H native:** trend -1, RSI 42.1, `in_demand 1` (flushed into 4H demand, `dist_demand_atr 0.58`), `clean_sky_atr 0.18` (limited immediate room), `choch_rec 1` (recent 4H structure shift). 15M flush lands on 4H demand (partial Angle 5 L5.6 nested-floor), but overhead is not clean.
- **1D native:** trend -1, RSI 44.0, `in_demand 1`, `dist_demand_atr -0.12` (just under daily demand), `hd_pos 0.32` (lower-mid of daily range), `hd_rsi 41.8`. Daily still bearish but mid-range, not crashed.
- **Net:** all HTF frames bearish (15M counter-trend long into a multi-TF down-leg), price in the discount third (`dealing_range_pos -0.271`), flushed into stacked 4H+1D demand. The reversal is a **deep-value Asia stop-run snap** off a defended demand floor, against the prevailing HTF trend — captured by the local sweep-reclaim-thrust mechanic, not by HTF momentum.

# Fund 60 — DEEP READING (XAU 15M MON+FORTE bottom)

- **Date:** 2024-09-19 01:30 UTC (block 2024-08-25), `t=1726709400`
- **Tier:** FORTE | power_score 12.8 | leg_atr 16.18
- **Year:** 2024 | Session: **ASIA** | killzone: **0** (off-killzone)
- **Outcome geometry:** mfe12 = **5.8 ATR**, mae12 = **0.46 ATR** (almost no adverse excursion after entry)

---

## (a) ENTRY MECHANIC — where/when I would actually enter

**Entry: at the close of reaction bar 2 (EMA21 reclaim confirmation).** This is a **sweep + reclaim + immediate CHoCH** bottom, the cleanest variety in this corpus.

The causal sequence I can act on, bar by bar (only info up to the trigger):
1. **Low bar (i):** price swept a prior fractal low (`swept_prior_low=1`) with a *shallow* sweep (`sweep_depth_atr=1.94`, near the MONFORTE-typical shallow band, well under the deep control flushes). The down-leg into it was **grindy, not impulsive** (`downleg_eff=0.0`, `consec_down=1`, `flush_v_ratio=0.32` = sharp local V). RSI at the low only 41.5 / min8 38.6 — **NOT deeply oversold** (the counter-intuitive MONFORTE fingerprint).
2. **Reaction bar 1:** instantly green, closes +1.59 ATR off the low, bar low already at +0.46 ATR — a decisive thrust bar (LENS-4-geometry `pivot_engulf_thrust`). `first_higher_low_bar=1` confirms a higher-low formed on the very first bar.
3. **Reaction bar 2 (ENTRY):** EMA21 reclaimed (`reclaim_ema_bars=2`), close +3.09 ATR, bar low climbs to +1.60 ATR. **15M bullish CHoCH printed (`choch_15m_after=1`).** This is my trigger: sweep of resting liquidity → instant reclaim → EMA back above → structure break up. Enter on the bar-2 close.
4. **Confirmation (would NOT have been needed but validates):** bars 3-4 continue the monotone staircase (c_atr 3.80→4.92), the floor keeps rising (l_atr 2.95→3.81). The "no-look-back" launch.

Stop logic: below the swept low (the MAE never exceeded 0.46 ATR, so a stop just under the low would never have been threatened). This is a **let-run** profile — mfe 5.8 ATR, the leg ran clean.

NAS-LONG did NOT confirm (`nas_long_after=0`, `nas_long_16=0`) — this bottom is carried by **structure (sweep+reclaim+CHoCH)**, not by a NAS signal.

---

## (b) Lenses PRESENT / STRONG here

**Angle 4 — Inter-bar geometry (THE dominant read):**
- **L1 `reclaim_low_monotone_k` = STRONG (run = 4/4).** Lows climb every reaction bar: 0.46→1.60→2.95→3.81. Textbook climbing-floor / no-look-back launch.
- **L2 `reclaim_jerk` / front-loaded = STRONG.** Per-bar velocity 1.59 → 1.50 → 0.71 → 1.12 — the first two bars do the bulk of the reclaim (impulsive spring release), then it eases.
- **L5 `close_progression_R2` = STRONG.** c_atr 1.59/3.09/3.80/4.92 is a near-straight rising ramp (high R², monotone) — one-directional control, no two-way auction.
- **L6 `pivot_engulf_thrust` = STRONG.** Bar 1 = +1.59 ATR thrust off a 0.46 low = decisive engulfing turn bar.
- **L8 `reclaim_dip_depth` = STRONG (shallow retest).** The only pullback (bar 11 dips l_atr to 3.14 vs the 5.8 high) holds far above the low — the higher-low quality is excellent. MAE 0.46 confirms.
- **L9 `velocity_regime_flip` = STRONG.** Steep down-slope inverts hard into a steep up-slope.

**Angle 1 / Angle 3 — Liquidity + Time (the entry trigger half):**
- **Angle1 L1 `QUIET RECLAIM` = PRESENT.** `killzone=0` + ASIA session + shallow non-headline sweep = the off-killzone quiet-reclaim signature (the 8.1× lift probe). 01:30 UTC is squarely in the Asia ramp.
- **Angle3 L1/L3/L4 = PRESENT.** ASIA off-peak (49% MONFORTE enrichment), bottom prints ~1.5h into the Asia session (first-session-hour ramp), `swept_prior_low=1` = cross-session overnight-low sweep reclaim.

**Angle 5 — Cross-TF (mixed, but the *spring* is real):**
- **L5.5 `flush-spike isolation` = PRESENT.** `flush_v_ratio=0.32` (sharp 15M V) into 4H demand context; the 15M panic is a wick, not an HTF body.
- **L5.4 compressed-regime: PARTIAL.** `atr_regime=0.75` (LOW — compressed, the MONFORTE side) and `atr_compression_pre=2.92` (strongly coiled). Strong vol-coil precondition.
- **L5.1/L5.2 phase-lag: INVERTED here.** `h1_trend=-1`, `h4_trend=0`, h1 below demand (`h1_pos=0.10`, `dist_demand_atr` deeply negative). The native-15M-resample 1H is still bearish — this is NOT the "1H-leads" variety. **BUT** `htf1_native.trend=+1` (the separate native-1H field) IS bullish, and `htf1_native.rsi=62.2` (strong, hooking) — so on the native-1D-leaning frame momentum is constructive. This bottom leans on **vol-coil + structure**, not on cross-TF trend alignment.

**Angle 0 / Angle 2 — Order-flow + Volatility:**
- **A0 L2 `quiet_climax` = STRONG (3/3):** vol_climax 1.25 (<1.35), sweep 1.94 (~shallow band), lower_wick 0.28 (<0.45). The MONFORTE anti-capitulation fingerprint.
- **A0 L10 / A2 thesis = PRESENT:** RSI not oversold (41.5) while making the low + grindy downleg (`downleg_eff=0`) = momentum absorbed, low-energy controlled descent into a coiled pocket, then spring.
- `atr_regime 0.75` + `atr_compression_pre 2.92` = the **drained-and-coiled** state Angle 2 prizes.

**Caution (honest):** `sell_bub_w=2`, `sell_decel=2`, `buy_bub_w=0`, `nas_long_16=0`. Order-flow bubble footprint is thin — no BUY-bubble first-print, modest sell-decel. The bullish read here is geometry + liquidity + vol-coil, NOT bubble/NAS confluence.

---

## (c) What is DISTINCTIVE about this bottom

1. **A near-perfect monotone staircase launch.** Run-4/4 climbing floor + near-straight close ramp + front-loaded thrust + MAE only 0.46 ATR. This is the prototype the Angle-4 author flagged ("no-look-back"). The trajectory shape, not any single scalar, is the tell.
2. **It bottoms with RSI ~41, not washed.** Most bottom detectors require oversold; this one isn't (`rsi_low 41.5`, `rsi_min8 38.6`). Momentum was absorbed quietly, not capitulated — the counter-intuitive MONFORTE signature.
3. **Quiet, off-killzone Asia stop-run.** 01:30 UTC, killzone=0, shallow sweep — an engineered overnight-low raid in thin liquidity that snaps back, not a London/NY crowd flush.
4. **Strongly coiled vol** (`atr_compression_pre 2.92`, `atr_regime 0.75`) → spring released cleanly. Drained-then-expand.
5. **Mechanic is purely structural** (sweep→reclaim→EMA→CHoCH) with **no NAS/bubble confirmation** — a reminder that the cleanest legs can come from geometry+liquidity alone.

---

## (d) Macro / HTF context

- **15M-native HTF (features_E1):** both fast frames technically still down/flushed — `h1_trend=-1`, `h4_trend=0`, price well below both demands (`h1_dist=-5.24`, `h4_dist=-5.61`), `h1_pos`/`h4_pos`=0.10. So the 15M flush is a deep stab below the intraday HTF floors.
- **Native resample HTF (htf*_native):** the *bigger* picture is constructive — `htf1_native.trend=+1`, `htf1_native.rsi=62.2`, `htf4_native.rsi=52.9` with `htf4_native.dist_demand_atr=2.71` and `clean_sky_atr=1.67` (room overhead), `htf1_native.clean_sky_atr=99` (clear sky on the slow frame). `macro_bull=0/macro_bear=0` (no regime label either way).
- **Reading:** a 15M-only liquidity-grab spike INTO intraday HTF demand, occurring inside a compressed higher-TF vol regime with the slow frame already bullish and clear sky above — a multi-TF spring with a floor below and air to run into. The leg delivered 5.8 ATR with virtually no heat.
